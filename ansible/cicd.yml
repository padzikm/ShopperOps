- hosts: localhost
  connection: local
  remote_user: vagrant
  become: true
  pre_tasks:
    - import_role: 
        name: system
        tasks_from: timedate
  roles:
    - powershell
    - docker
    - docker-compose
    - registry
    - portainer
    - jenkins-master
    - jenkins-slave